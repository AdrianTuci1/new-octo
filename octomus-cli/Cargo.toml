[package]
name = "octomus-cli"
version = "0.1.0"
edition = "2021"
description = "Lightweight CLI tool for the Octomus Launcher"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
