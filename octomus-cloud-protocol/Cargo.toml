[package]
name = "octomus-cloud-protocol"
version = "0.1.0"
edition = "2021"
description = "Shared cloud run protocol types for Octomus runtimes"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
