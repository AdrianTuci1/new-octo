Repo: AdrianTuci1/new-octo
Base branch: codex/launcher-context-refactor
Task: state management + base widgets in octomus-ui

~> git clone https://oauth2:${GITHUB_TOKEN}@github.com/AdrianTuci1/new-octo.git /tmp/repo
~> cd /tmp/repo
~> git checkout codex/launcher-context-refactor
~> git checkout -b hermes/ui-state-mgmt

---

CRITICAL: NEVER use write_file. ALWAYS write files with bash heredoc:
cat > path/to/file << 'EOF'
content
EOF

Creeaza octomus-ui/ de la zero cu cat heredoc.

1. octomus-ui/Cargo.toml:
cat > octomus-ui/Cargo.toml << 'RUSTEOF'
[package]
name = "octomus-ui"
version = "0.1.0"
edition = "2021"
[dependencies]
egui = "0.31"
eframe = "0.31"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
octomus-core = { path = "../octomus-core" }
RUSTEOF

2. octomus-ui/src/lib.rs — module declarations
3. octomus-ui/src/state/mod.rs + chat.rs + launcher.rs + runtime.rs + ui.rs + editor.rs + shell.rs + agent.rs + memory.rs + model_selection.rs
4. octomus-ui/src/components/mod.rs + button.rs + input.rs + tabs.rs + panel.rs + drawer.rs + scroll_area.rs + icon.rs + badge.rs + tooltip.rs + context_menu.rs

Update root Cargo.toml: add "octomus-ui" to workspace members.

~> cargo check 2>&1 | head -50 || true
~> git add .
~> git commit -m "state-mgmt: port Zustand stores + base widgets"
~> git push origin hermes/ui-state-mgmt
